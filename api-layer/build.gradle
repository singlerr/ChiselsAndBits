plugins {
    id 'java'
}

group = rootProject.maven_group
version = rootProject.mod_version
base {
    archivesName = "${archives_base_name}-api-layer"
}
repositories {
    mavenCentral()
}

dependencies {
}

test {
    useJUnitPlatform()
}